# Dependencies
node_modules/
.pnpm-store/

# Env / secrets
.env
.env.*
!.env.example
*.pem

# Builds
dist/
build/
.expo/
.expo-shared/
web-build/
coverage/
*.tsbuildinfo

# Native
ios/Pods/
android/.gradle/
android/app/build/

# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

# Content ingest: a series is assembled here before it replaces the published one
.ingest-stage/
# Media on R2: what is already on the store, per episode (docs/cloud-ingest.md)
.ingest-records/
# Compilation splitter proposals and deliveries downloaded on a runner
.split-work/
.delivery/
# Social clips: the videos, the frames, and clips.json — which IS the memory of
# what was already cut, so deleting a series' folder is how you ask for it again
clips/

# Logs / temp
*.log
npm-debug.log*
tmp/
.tmp/

# Misc
*.tgz
.vercel
expo-env.d.ts
